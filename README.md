# general-config
Setting up and updating configuration on Git, Docker, bash

## .gitignore
used to decide which pattern of file will not be tracked by git

## .dockerignore
when building images, we don't include some files defined in .dockerignore

## git config 
Setting personal information in config , then put it in Home_dir
